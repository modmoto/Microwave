fromCategory('SeasonWrite')
.foreachStream()
.when(
{
  $any : function(s,ev) { linkTo('SeasonRead-' + ev.eventType, ev);
}
});

fromCategory('SeasonWriteTest')
.foreachStream()
.when(
{
$any : function(s,ev) { linkTo('SeasonReadTest-' + ev.eventType, ev);
}
});